{
  final int padding=icon.getPressedOrFocusedBackgroundPadding();
  invalidate(icon.getLeft() + getLeftPadding() - padding,icon.getTop() + getTopPadding() - padding,icon.getRight() + getLeftPadding() + padding,icon.getBottom() + getTopPadding() + padding);
}
