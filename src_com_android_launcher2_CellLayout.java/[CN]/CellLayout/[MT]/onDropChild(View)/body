{
  if (child != null) {
    LayoutParams lp=(LayoutParams)child.getLayoutParams();
    lp.isDragging=false;
    lp.dropped=true;
    child.setVisibility(View.VISIBLE);
    child.requestLayout();
  }
}
