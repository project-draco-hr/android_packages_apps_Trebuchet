import cgi
import os
import shutil
import sys
import sqlite3
SCREENS = 5
COLUMNS = 4
ROWS = 4
HOTSEAT_SIZE = 5
CELL_SIZE = 110
DIR = 'db_files'
AUTO_FILE = (DIR + '/launcher.db')
INDEX_FILE = (DIR + '/index.html')
FUNCTIONS = {'intent': print_intent, 'icon': print_icon, }
if (__name__ == '__main__'):
    main(sys.argv)
