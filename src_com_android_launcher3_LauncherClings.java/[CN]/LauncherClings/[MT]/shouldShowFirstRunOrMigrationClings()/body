{
  SharedPreferences sharedPrefs=mLauncher.getSharedPrefs();
  return isClingsEnabled() && !sharedPrefs.getBoolean(FIRST_RUN_CLING_DISMISSED_KEY,false) && !sharedPrefs.getBoolean(MIGRATION_CLING_DISMISSED_KEY,false);
}
