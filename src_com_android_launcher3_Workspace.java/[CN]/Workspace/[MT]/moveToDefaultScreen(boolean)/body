{
  if (!isSmall()) {
    if (animate) {
      snapToPage(mDefaultPage);
    }
 else {
      setCurrentPage(mDefaultPage);
    }
  }
  View child=getChildAt(mDefaultPage);
  if (child != null) {
    child.requestFocus();
  }
}
