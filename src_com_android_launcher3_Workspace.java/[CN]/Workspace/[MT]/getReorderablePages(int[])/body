{
  int count=mScreenOrder.size();
  int start=-1;
  int end=-1;
  for (int i=0; i < count; i++) {
    if (start < 0 && mScreenOrder.get(i) >= 0) {
      start=i;
    }
    if (start >= 0 && mScreenOrder.get(i) >= 0) {
      end=i;
    }
  }
  range[0]=start;
  range[1]=end;
}
