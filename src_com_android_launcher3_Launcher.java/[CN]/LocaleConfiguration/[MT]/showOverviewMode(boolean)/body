{
  mWorkspace.setVisibility(View.VISIBLE);
  mStateTransitionAnimation.startAnimationToWorkspace(mState,Workspace.State.OVERVIEW,animated,null);
  mState=State.WORKSPACE;
  onWorkspaceShown(animated);
}
