{
  if (mState == State.APPS_SPRING_LOADED) {
    mStateTransitionAnimation.startAnimationToAllApps(true);
    mState=State.APPS;
  }
 else   if (mState == State.WIDGETS_SPRING_LOADED) {
    mStateTransitionAnimation.startAnimationToWidgets(true);
    mState=State.WIDGETS;
  }
}
