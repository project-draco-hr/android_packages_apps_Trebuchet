{
  if (mDropAnim != null) {
    mDropAnim.cancel();
  }
  if (mDropView != null) {
    final DragView dropView=mDropView;
    post(new Runnable(){
      @Override public void run(){
        mDragController.onDeferredEndDrag(dropView);
      }
    }
);
  }
  mDropView=null;
  invalidate();
}
