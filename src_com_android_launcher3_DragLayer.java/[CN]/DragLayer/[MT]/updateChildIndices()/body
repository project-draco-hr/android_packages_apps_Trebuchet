{
  mTopViewIndex=-1;
  int childCount=getChildCount();
  for (int i=0; i < childCount; i++) {
    if (getChildAt(i) instanceof DragView || getChildAt(i) == mOverlayView) {
      mTopViewIndex=i;
    }
  }
  mChildCountOnLastUpdate=childCount;
}
