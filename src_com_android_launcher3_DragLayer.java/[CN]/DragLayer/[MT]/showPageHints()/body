{
  mShowPageHints=true;
  Workspace workspace=mLauncher.getWorkspace();
  getDescendantRectRelativeToSelf(workspace.getChildAt(workspace.getChildCount() - 1),mScrollChildPosition);
  invalidate();
}
