{
  final ContentValues values=new ContentValues();
  final ContentResolver cr=context.getContentResolver();
  item.onAddToDatabase(values);
  item.updateValuesWithCoordinates(values,item.cellX,item.cellY);
  sWorker.post(new Runnable(){
    public void run(){
      cr.update(LauncherSettings.Favorites.getContentUri(item.id,false),values,null,null);
      final ItemInfo modelItem=sItemsIdMap.get(item.id);
      if (item != modelItem) {
        Log.e(TAG,"item: " + ((item != null) ? item.toString() : "null"));
        Log.e(TAG,"modelItem: " + ((modelItem != null) ? modelItem.toString() : "null"));
        throw new RuntimeException("Error: ItemInfo passed to updateItemInDatabase " + "doesn't match original");
      }
    }
  }
);
}
