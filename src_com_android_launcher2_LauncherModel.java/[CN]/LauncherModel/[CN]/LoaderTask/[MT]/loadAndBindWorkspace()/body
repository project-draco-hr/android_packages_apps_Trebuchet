{
  if (DEBUG_LOADERS) {
    Log.d(TAG,"loadAndBindWorkspace mWorkspaceLoaded=" + mWorkspaceLoaded);
  }
  if (!mWorkspaceLoaded) {
    loadWorkspace();
    if (mStopped) {
      return;
    }
    mWorkspaceLoaded=true;
  }
  bindWorkspace();
}
