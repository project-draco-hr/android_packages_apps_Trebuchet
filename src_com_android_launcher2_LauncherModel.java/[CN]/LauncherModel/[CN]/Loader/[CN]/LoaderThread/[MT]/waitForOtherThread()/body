{
  if (mWaitThread != null) {
    boolean done=false;
    while (!done) {
      try {
        mWaitThread.join();
      }
 catch (      InterruptedException ex) {
      }
    }
    mWaitThread=null;
  }
}
