{
  if (!v.isInTouchMode())   return false;
  if (mNextPage != INVALID_PAGE)   return false;
  if (!mLauncher.isAllAppsCustomizeOpen() || mLauncher.getWorkspace().isSwitchingState())   return false;
  return beginDragging(v);
}
