{
  mIcons[index]=Allocation.createFromBitmap(sRS,item.iconBitmap,Element.RGBA_8888(sRS),false);
  mLabels[index]=Allocation.createFromBitmap(sRS,item.titleBitmap,Element.A_8(sRS),false);
  mIconIds[index]=mIcons[index].getID();
  mLabelIds[index]=mLabels[index].getID();
}
