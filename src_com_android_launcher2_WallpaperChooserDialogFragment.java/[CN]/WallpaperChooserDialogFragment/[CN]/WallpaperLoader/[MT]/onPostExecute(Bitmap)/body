{
  if (b == null)   return;
  if (!isCancelled() && !mOptions.mCancel) {
    if (mBitmap != null) {
      mBitmap.recycle();
    }
    mBitmap=b;
    mWallpaperDrawable.setBitmap(b);
    getView().postInvalidate();
    mLoader=null;
  }
 else {
    b.recycle();
  }
}
