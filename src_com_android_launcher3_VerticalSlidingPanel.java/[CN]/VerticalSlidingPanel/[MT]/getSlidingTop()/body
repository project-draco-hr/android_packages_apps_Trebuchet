{
  if (mSlideableView != null) {
    return mIsSlidingUp ? getMeasuredHeight() - getPaddingBottom() - mSlideableView.getMeasuredHeight() : getPaddingTop();
  }
  return getMeasuredHeight() - getPaddingBottom();
}
