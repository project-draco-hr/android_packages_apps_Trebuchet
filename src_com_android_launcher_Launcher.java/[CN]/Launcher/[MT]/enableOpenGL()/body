{
  if (USE_OPENGL && "true".equals(SystemProperties.get(PROPERTY_USE_OPENGL,USE_OPENGL_BY_DEFAULT))) {
    if (Config.LOGD) {
      Log.d(LOG_TAG,"Launcher starting in OpenGL");
    }
  }
 else {
    sOpenGlEnabled=false;
  }
}
