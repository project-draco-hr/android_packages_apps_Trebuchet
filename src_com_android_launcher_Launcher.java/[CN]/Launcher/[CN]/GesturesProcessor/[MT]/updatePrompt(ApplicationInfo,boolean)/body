{
  if (mGesturesAction.intent != null && info.intent.toUri(0).equals(mGesturesAction.intent.toUri(0)) && info.title.equals(((TextView)mGesturesPrompt.getCurrentView()).getText())) {
    return;
  }
  if (animate) {
    setGesturesNextPrompt(info.icon,info.title);
  }
 else {
    setGesturesPrompt(info.icon,info.title);
  }
  mGesturesAction.intent=info.intent;
}
