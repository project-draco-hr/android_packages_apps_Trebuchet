{
  mLauncher.bindPackagesUpdated(LauncherModel.getSortedWidgetsAndShortcuts(mLauncher,true));
  if (!mProviderChangeListeners.isEmpty()) {
    for (    Runnable callback : new ArrayList<>(mProviderChangeListeners)) {
      callback.run();
    }
  }
}
