{
  if (ev.getAction() == MotionEvent.ACTION_DOWN) {
    if (mLauncher.isWorkspaceLocked() || (!LauncherApplication.isScreenXLarge() && mLauncher.isAllAppsVisible())) {
      return false;
    }
  }
  return super.dispatchTouchEvent(ev);
}
