{
  int result=-1;
  if (v != null) {
    ViewParent vp=v.getParent();
    final int screenCount=getChildCount();
    for (int i=0; i < screenCount; i++) {
      if (vp == getChildAt(i)) {
        return i;
      }
    }
  }
  return result;
}
