{
  mIsDragOccuring=true;
  updateChildrenLayersEnabled();
  mLauncher.lockScreenOrientationOnLargeUI();
  animate().alpha(mDragFadeOutAlpha).setInterpolator(new AccelerateInterpolator()).setDuration(mDragFadeOutDuration).start();
}
