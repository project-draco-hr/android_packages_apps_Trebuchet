{
  mIsDragOccuring=true;
  updateChildrenLayersEnabled();
  mLauncher.lockScreenOrientationOnLargeUI();
  animate().alpha(mDragFadeOutAlpha).setInterpolator(new AccelerateInterpolator(1.5f)).setDuration(mDragFadeOutDuration).start();
}
