{
  final int screenCount=getChildCount();
  for (int i=0; i < screenCount; i++) {
    CellLayout cl=(CellLayout)getChildAt(i);
switch (state) {
case SHRINK_TO_TOP:
      if (!isDragHappening) {
        boolean showDropHighlight=i == mCurrentPage;
        cl.setAcceptsDrops(showDropHighlight);
        break;
      }
case SHRINK_TO_BOTTOM_HIDDEN:
case SHRINK_TO_BOTTOM_VISIBLE:
    if (!isDragHappening) {
      spanX=1;
      spanY=1;
    }
  cl.setAcceptsDrops(cl.findCellForSpan(null,spanX,spanY));
break;
default :
throw new RuntimeException("updateWhichPagesAcceptDropsHelper passed an unhandled ShrinkPosition");
}
}
}
