{
  mIsDragOccuring=false;
  updateChildrenLayersEnabled();
  mLauncher.unlockScreenOrientationOnLargeUI();
  animate().alpha(1f).setInterpolator(new AccelerateInterpolator()).setDuration(mDragFadeOutDuration).start();
}
