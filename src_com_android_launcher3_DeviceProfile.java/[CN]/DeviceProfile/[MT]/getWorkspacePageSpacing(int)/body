{
  if ((orientation == CellLayout.LANDSCAPE && transposeLayoutWithOrientation) || isLargeTablet()) {
    return defaultPageSpacingPx;
  }
 else {
    return Math.max(defaultPageSpacingPx,2 * getWorkspacePadding().left);
  }
}
