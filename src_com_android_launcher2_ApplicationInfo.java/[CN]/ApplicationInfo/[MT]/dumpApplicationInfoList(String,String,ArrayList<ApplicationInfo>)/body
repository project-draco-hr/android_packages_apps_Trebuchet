{
  Log.d(tag,label + " size=" + list.size());
  for (  ApplicationInfo info : list) {
    Log.d(tag,"   title=\"" + info.title + "\" icon="+ info.icon+ " iconBitmap="+ info.iconBitmap+ " filtered="+ info.filtered+ " customIcon="+ info.customIcon);
  }
}
