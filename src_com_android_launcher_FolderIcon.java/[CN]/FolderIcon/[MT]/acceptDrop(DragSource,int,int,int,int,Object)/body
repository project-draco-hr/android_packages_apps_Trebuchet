{
  final ItemInfo item=(ItemInfo)dragInfo;
  final int itemType=item.itemType;
  return (itemType == Settings.Favorites.ITEM_TYPE_APPLICATION || itemType == Settings.Favorites.ITEM_TYPE_SHORTCUT) && item.container != mInfo.id;
}
