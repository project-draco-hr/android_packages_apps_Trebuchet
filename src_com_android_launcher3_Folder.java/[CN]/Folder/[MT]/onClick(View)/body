{
  Object tag=v.getTag();
  if (tag instanceof ShortcutInfo) {
    mLauncher.onClick(v);
  }
  if (v.getId() == R.id.folder_lock) {
    startHiddenFolderManager(mInfo.hidden ? Launcher.REQUEST_UNPROTECT_FOLDER : Launcher.REQUEST_PROTECT_FOLDER);
  }
}
