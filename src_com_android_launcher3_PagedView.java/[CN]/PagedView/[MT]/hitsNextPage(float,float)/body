{
  int offset=(getViewportWidth() - getChildWidth(mCurrentPage)) / 2;
  if (isLayoutRtl()) {
    return (x < getViewportOffsetX() + offset);
  }
  return (x > (getViewportOffsetX() + getViewportWidth() - offset));
}
