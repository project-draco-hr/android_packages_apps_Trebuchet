{
  if (LauncherApplication.isScreenXLarge()) {
    return FocusHelper.handlePagedViewWidgetKeyEvent(this,keyCode,event) || super.onKeyUp(keyCode,event);
  }
 else {
    return FocusHelper.handlePagedViewGridLayoutWidgetKeyEvent(this,keyCode,event) || super.onKeyUp(keyCode,event);
  }
}
