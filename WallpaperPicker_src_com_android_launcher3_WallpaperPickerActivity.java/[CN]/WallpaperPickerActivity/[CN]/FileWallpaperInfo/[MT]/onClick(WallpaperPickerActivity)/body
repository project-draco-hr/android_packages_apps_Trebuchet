{
  BitmapRegionTileSource.UriBitmapSource bitmapSource=new BitmapRegionTileSource.UriBitmapSource(a.getContext(),Uri.fromFile(mFile),1024);
  a.setCropViewTileSource(bitmapSource,false,true,null,null);
}
