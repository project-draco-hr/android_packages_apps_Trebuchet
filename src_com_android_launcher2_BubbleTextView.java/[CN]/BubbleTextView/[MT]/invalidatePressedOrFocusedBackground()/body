{
  int padding=HolographicOutlineHelper.MAX_OUTER_BLUR_RADIUS / 2;
  View parent=(View)getParent();
  if (parent != null) {
    parent.invalidate(getLeft() - padding,getTop() - padding,getRight() + padding,getBottom() + padding);
  }
  invalidate();
}
