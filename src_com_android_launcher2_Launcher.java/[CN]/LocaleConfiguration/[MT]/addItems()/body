{
  if (LauncherApplication.isScreenXLarge()) {
    if (!isCustomizationDrawerVisible()) {
      showCustomizationDrawer(true);
    }
  }
 else {
    closeAllApps(true);
    showAddDialog(-1,-1);
  }
}
